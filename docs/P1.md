# P1 — Cryptography & Protocol (Complete)

## Deliverables

| Package | Status |
|---------|--------|
| `@dpe/proto` | Zod schemas: AuthEnvelope, JwtPayload, SignedUpdate, KeyRotation, Operable RPC |
| `@dpe/crypto` | Ed25519 NodeID, JWT (EdDSA), sealed `doc_key`, AES-GCM, SignedUpdate, ReplayCache |
| `@dpe/acl` | Merge policy helpers (role 0–3); per-doc ACL independent of parent (RBAC §2.2) |
| `apps/web` | Onboarding generates real Ed25519 identity (UID = hex SHA-256(pk)) |

## Verify

```bash
pnpm test:p1
# or
node scripts/verify-p1.mjs
```

## Mapping to 方案 §7 (P1 scope)

- [x] Node Ed25519 key pair & NodeID
- [x] JWT sign/verify, `doc_key` sealed (not plaintext in JWT)
- [x] Per-doc AES-256-GCM + nonce + SignedUpdate + Ed25519 signature
- [x] Replay cache `(signer_node_id, seq)`
- [x] Merge policy helpers (role ≥ 2 to write)
- [ ] KeyRotation orchestration → **P2** (control-plane)
- [x] WebRTC AuthEnvelope wire → **P3** (`@dpe/p2p`)
- [x] Yjs SignedUpdate + merge guards → **P4** (`@dpe/yjs-provider`)

## Next: P2

Control plane + PostgreSQL: groups, invitations, JWT issuance API, `control_mode` (proxy / owner_direct).
